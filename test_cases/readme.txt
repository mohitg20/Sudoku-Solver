This folder has sudoku generated by generator and there solution

File name convention:
    <k>_<random number>_<q(question)/s(solution)>.csv           # these all satisfy diagonal constraint
        
NOTE: Here random number is actually time when script is called

Other files are manually added like input<x>.csv. In these, input2.csv, input3.csv and input5.csv are invalid files.